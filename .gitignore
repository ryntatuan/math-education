# Dependencies
node_modules/
*/node_modules/

# Production builds
dist/
dist-ssr/
*/dist/
*.apk
!client/public/downloads/ToanVui.apk
!client/public/ToanVui.apk

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment / local
.env
.env.local
.env.*.local
*.local

# System & Editor
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Agent & temporary tools
.codegraph/
.gemini/
supabase/.temp/


# Documentation
# (README.md is tracked)


# Output tạm của công cụ kiểm thử
scratch/portal-test-output.txt
scratch/visual-bundle.*
scratch/visual-that.cjs
scratch/cau-truc-slide.json
scratch/in-gon-*.txt
scratch/migrate-*-out.txt
scratch/verify-*.txt
scratch/*.log
scratch/gates.txt
scratch/loi-*.txt
# Bản đóng gói .cjs của các công cụ trong scratch/ (sinh lại bằng esbuild, không cần lưu)
scratch/*.cjs
# Kết quả chữ của công cụ soát hình / in bài (chạy lại là có, không cần lưu)
scratch/soat-kq.txt
scratch/soat-hinh-sai.txt
scratch/in-bai.txt
scratch/in-bai2.txt
# Thư viện hình để đo/ngắm: giữ file .jsx, bỏ bản đóng gói và trang sinh ra
scratch/tao-thu-vien-hinh.cjs
scratch/thu-vien-hinh.html
scratch/nhin-*.png
# Trang đo HÌNH THẬT (mọi ca hình có trong dữ liệu 5 lớp) — giữ .jsx, bỏ bản đóng gói + trang
scratch/visual-fit.cjs
scratch/visual-fit.html
# Bản sao lưu dữ liệu bài học trước khi tiêm hình (lấy lại được từ lịch sử git)
scratch/*.truoc-hinh.js
client/src/data/*.bak

# SGK scans & screenshot artifacts
scripts/sgk_g1_p*.png
scripts/page_*.png
scripts/g1c*.png
scripts/slide_*.png
scratch/*.png
# Ảnh trang SGK trích ra để đối chiếu bằng mắt (sinh lại: scratch/trich-anh-sgk.py)
scratch/sgk-*/

# Bản sao lưu dữ liệu lúc tách theo chương (lấy lại được từ lịch sử git)
scratch/truoc-tach/
# Bản đóng gói esbuild + kết quả đo tạm trong scratch/ (chạy lại là có, giữ lại script)
scratch/*.bundle.mjs
scratch/*.txt
scratch/cau-truc-lop*.md
# Trang xem trước hình (giữ file .jsx, bỏ trang sinh ra bằng esbuild)
scratch/xem-truoc-*.html


# Thu muc thu vien noi vao scratch de esbuild phan giai duoc react (junction, khong phai ma nguon)
scratch/node_modules/
